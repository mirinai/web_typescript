console.log("타입스크립트");

const cookie = {
  name: "white cookie",
  price: 2500,
};
console.log(`${cookie.name}'s price is ${cookie.price}`);

//npm install -g typescript
//npm install -g ts-node
